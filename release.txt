# to avoid overwriting NVS, generate hex
HEX_NAME="esp32_c3_imu_v1.hex"

idf.py merge-bin --format hex -o ${HEX_NAME}

# to flash
esptool.py --chip esp32c3 --port /dev/ttyACM0 write_flash 0x0 build/${HEX_NAME}
